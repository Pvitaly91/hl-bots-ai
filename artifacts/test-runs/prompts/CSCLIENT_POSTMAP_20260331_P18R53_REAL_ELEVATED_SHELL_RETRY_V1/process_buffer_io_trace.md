# Process Buffer I/O Trace

Process-specific `*buffer*.dat` tracing status: `not executed`

Reason:
- No direct ETW preflight was allowed.
- No traced rerun was allowed.

Recorded values:
- `traced_pid = none`
- `buffer_io_events = none`
